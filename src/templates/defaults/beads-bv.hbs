{{!--
  ABOUTME: Prompt template for Beads + bv (graph-aware) tracker.
  Includes extra context from bv's intelligent task selection.

  Available variables:
  - {{taskId}}, {{taskTitle}}, {{taskDescription}}, {{acceptanceCriteria}}
  - {{epicId}}, {{epicTitle}}, {{trackerName}}
  - {{labels}}, {{priority}}, {{status}}, {{type}}
  - {{dependsOn}}, {{blocks}}
  - {{model}}, {{agentName}}, {{cwd}}
  - {{currentDate}}, {{currentTimestamp}}
  - {{recentProgress}} - Summary of completed work from previous iterations
  - {{beadsDbPath}} - Full path to beads database (for bd --db flag)
--}}
## Bead Details - **ID**:
{{taskId}}
- **Title**:
{{taskTitle}}
{{#if epicId}}
  - **Epic**:
  {{epicId}}{{#if epicTitle}} - {{epicTitle}}{{/if}}
{{/if}}
{{#if taskDescription}}
  - **Description**:
  {{taskDescription}}
{{/if}}

{{#if acceptanceCriteria}}
  ## Acceptance Criteria
  {{acceptanceCriteria}}
{{/if}}

{{#if dependsOn}}
  ## Dependencies This task depends on:
  {{dependsOn}}
{{/if}}

{{#if blocks}}
  ## Impact Completing this task will unblock:
  {{blocks}}
{{/if}}

{{#if recentProgress}}
  ## Recent Progress
  {{recentProgress}}
{{/if}}

## Instructions 1. Implement the requirements (stay on current branch) 2. Run
your project's quality checks (typecheck, lint, etc.) 3. Commit: feat:
{{taskId}}
-
{{taskTitle}}
4. Close the bead when done: bd close
{{taskId}}
--db
{{beadsDbPath}}
--reason "Brief description" When finished, signal completion with:
<promise>COMPLETE</promise>