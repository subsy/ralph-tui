/**
 * ABOUTME: Sandbox type exports for runtime detection and configuration.
 * Re-exports sandbox configuration types for sandbox utilities.
 */

import type { SandboxConfig, SandboxMode } from '../config/types.js';

export type { SandboxConfig, SandboxMode };
